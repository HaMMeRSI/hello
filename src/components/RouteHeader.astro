---
import shLogo from '../assets/logo/sh.svg?url';
import { navItems, profile } from '../content/profile/site';

const headerLinks = navItems.filter(item => item.to !== '/');
---

<header class="route-header">
  <a href="/" class="route-logo" aria-label={`${profile.name} home`}>
    <img src={shLogo} alt="" aria-hidden="true" width="121" height="82" decoding="sync" fetchpriority="high" />
  </a>

  <nav class="route-links" aria-label="Primary navigation">
    {headerLinks.map(item => <a href={item.to} class="route-link">{item.label}</a>)}
  </nav>
</header>
